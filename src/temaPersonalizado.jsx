import { createTheme } from "@mui/material";

export const temaPersonalizado = createTheme({
  palette: {
    mode: "light",
    background: {
      default: '#E8E8E8'
    }
  },
  typography: {
    fontFamily: "Poppins"
  },
  spacing: 8,
});
